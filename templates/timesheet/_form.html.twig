{{ form_start(form) }}
<div class="form-row">
    <div class="form-group col-md-3">
        {{ form_row(form.workDay) }}
    </div>
</div>
<div class="form-row">
    <div class="form-group col-md-6">
        {{ form_row(form.comment, {attr: {'rows': 4}}) }}
    </div>
</div>
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
