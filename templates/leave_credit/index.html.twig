{% extends 'base.html.twig' %}

{% block title %}LeaveCredit index{% endblock %}

{% block content %}
    <h1>LeaveCredit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for leave_credit in leave_credits %}
            <tr>
                <td>{{ leave_credit.id }}</td>
                <td>
                    <a href="{{ path('leave_credit_show', {'id': leave_credit.id}) }}">show</a>
                    <a href="{{ path('leave_credit_edit', {'id': leave_credit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('leave_credit_new') }}">Create new</a>
{% endblock %}
