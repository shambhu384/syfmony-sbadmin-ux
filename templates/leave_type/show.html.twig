{% extends 'base.html.twig' %}

{% block title %}LeaveType{% endblock %}

{% block content %}
    <h1>LeaveType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leave_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ leave_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('leave_type_index') }}">back to list</a>

    <a href="{{ path('leave_type_edit', {'id': leave_type.id}) }}">edit</a>

    {{ include('leave_type/_delete_form.html.twig') }}
{% endblock %}
