{% extends 'base.html.twig' %}

{% block title %}Onboarding{% endblock %}

{% block content %}
    <h1>Onboarding</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ onboarding.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ onboarding.name }}</td>
            </tr>
            <tr>
                <th>Response</th>
                <td>{{ onboarding.response }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ onboarding.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('onboarding_index') }}">back to list</a>

    <a href="{{ path('onboarding_edit', {'id': onboarding.id}) }}">edit</a>

    {{ include('onboarding/_delete_form.html.twig') }}
{% endblock %}
